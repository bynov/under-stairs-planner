# Russian Language (i18n) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make every user-visible string (UI, 2D drawing labels, cut list, PDF) available in English and Russian, with an EN | RU switch persisted in localStorage and a Cyrillic-capable font embedded in the PDF.

**Architecture:** A dependency-free `src/i18n/` module with typed dictionaries and a pure `t(lang, key, params)`. Data that carries text (part names, notes, validation and import errors, toasts) becomes language-neutral `Msg = { key, params }` objects translated at render time. Views and the PDF builder take `lang`; the store holds `ui.lang`.

**Tech Stack:** existing stack (Vite, React 18, TS strict, zustand, jsPDF 4, vitest). PT Sans Regular TTF (OFL) embedded as a base64 module for jsPDF.

**Spec:** `docs/superpowers/specs/2026-09-07-understairs-closet-planner-design.md` (§2.6 Internationalisation, §3.1 Part, §3.3 font, §4 UI)

## Global Constraints

- `Lang = 'en' | 'ru'`. `en.ts` defines the key set; `ru.ts` is typed `Record<MessageKey, string>` so TypeScript enforces parity; a runtime test also checks parity and non-empty strings.
- `t(lang, key, params?)`: `{name}` placeholders replaced by `String(params[name])`; a missing param leaves the placeholder as-is; unknown lang key falls back to `en`.
- `Msg = { key: MessageKey; params?: Params }`; `tm(lang, msg)` translates it.
- Numbers keep dot decimals; units `mm`/`мм`; degrees rendered with `°`.
- Part names: `Part.nameKey` from `PartNameKey = 'sideL'|'sideR'|'top'|'bottom'|'back'|'shelf'|'door'|'drawerFront'|'fixedFront'|'plinth'|'rod'`, i18n key `part.<nameKey>`; `Part.index` 1-based for shelf/drawerFront; `Part.notes?: Msg[]`.
- Cut list groups by `(kind, nameKey, length, width, thickness, material, JSON(notes))`; `CutRow.nameKey`, `CutRow.notes: Msg[]`.
- `ValidationError.message: Msg`; `ParseResult` error is a `Msg`; `ui.toast: Msg | null`.
- Language persistence key `understairs-planner:lang`; default `detectLang(navigator.language)` (`ru*` → `'ru'`, else `'en'`).
- PDF font: PT Sans Regular, registered as family `PTSans` via `addFileToVFS` + `addFont`, used for every page. Source TTF and OFL licence committed under `src/pdf/fonts/`; base64 module generated by `scripts/embed-font.mjs`.
- No per-task commits (user instruction); one unsigned commit at the end. `pnpm typecheck && pnpm test && pnpm build` must pass after every task.
- Existing behaviour and numbers (geometry, dims, page counts) must not change; only the text changes.

## File Structure

```
src/i18n/en.ts             English dictionary (defines MessageKey)
src/i18n/ru.ts             Russian dictionary
src/i18n/index.ts          Lang, Params, Msg, msg(), t(), tm(), detectLang(), isLang()
src/i18n/i18n.test.ts
src/model/types.ts         ValidationError.message: Msg
src/model/validate.ts      messages -> Msg
src/store/persist.ts       ParseResult error: Msg; LANG_KEY, loadLang, saveLang
src/store/store.ts         ui.lang, setLang, toast: Msg; autosave also persists lang
src/geometry/parts.ts      nameKey/index/notes: Msg[]
src/cutlist/cutlist.ts     nameKey grouping
src/drawing/views.ts       lang parameter, translated labels/titles
src/pdf/fonts/PT_Sans-Web-Regular.ttf, OFL.txt, ptsans.ts (generated), font.ts (registerPdfFont)
scripts/embed-font.mjs     ttf -> base64 TS module
src/pdf/exportPdf.ts       lang option, translated summary/cut list, font
src/ui/useT.ts             hook: { lang, t, tm }
src/ui/*.tsx               translated components, language switch
tests updated alongside each file
```

---

### Task 1: i18n core

**Files:**
- Create: `src/i18n/en.ts`, `src/i18n/ru.ts`, `src/i18n/index.ts`
- Test: `src/i18n/i18n.test.ts`

**Interfaces:**
- Produces: `Lang`, `LANGS`, `MessageKey`, `Params`, `Msg`, `msg(key, params?)`, `t(lang, key, params?)`, `tm(lang, m)`, `detectLang(navLang?)`, `isLang(v)`.

- [ ] **Step 1: Write failing test src/i18n/i18n.test.ts**

```ts
import { describe, it, expect } from 'vitest';
import { en } from './en';
import { ru } from './ru';
import { t, tm, msg, detectLang, isLang, LANGS } from './index';

describe('dictionaries', () => {
  it('have identical key sets and no empty strings', () => {
    const ek = Object.keys(en).sort();
    const rk = Object.keys(ru).sort();
    expect(rk).toEqual(ek);
    for (const k of ek) {
      expect((en as Record<string, string>)[k].length).toBeGreaterThan(0);
      expect((ru as Record<string, string>)[k].length).toBeGreaterThan(0);
    }
  });
  it('russian strings contain Cyrillic where expected', () => {
    expect(ru['part.sideL']).toMatch(/[А-Яа-я]/);
    expect(ru['view.front']).toBe('Фасад');
  });
});

describe('t', () => {
  it('translates and interpolates', () => {
    expect(t('en', 'ui.column', { n: 2 })).toBe('Column 2');
    expect(t('ru', 'ui.column', { n: 2 })).toBe('Секция 2');
    expect(t('en', 'label.slope', { deg: 26.6 })).toBe('slope 26.6°');
  });
  it('leaves unknown placeholders and translates Msg objects', () => {
    expect(t('en', 'ui.column')).toBe('Column {n}');
    expect(tm('ru', msg('note.rodDia', { d: 25 }))).toBe('Ø25 мм');
  });
});

describe('detectLang / isLang', () => {
  it('maps ru locales to ru, everything else to en', () => {
    expect(detectLang('ru')).toBe('ru');
    expect(detectLang('ru-RU')).toBe('ru');
    expect(detectLang('en-GB')).toBe('en');
    expect(detectLang(undefined)).toBe('en');
    expect(isLang('ru')).toBe(true);
    expect(isLang('de')).toBe(false);
    expect(LANGS).toEqual(['en', 'ru']);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm vitest run src/i18n/i18n.test.ts`
Expected: FAIL, cannot resolve `./en`.

- [ ] **Step 3: Write src/i18n/en.ts**

```ts
export const en = {
  // top bar / tabs
  'ui.tab.3d': '3D',
  'ui.tab.front': 'Front',
  'ui.tab.plan': 'Plan',
  'ui.tab.side': 'Side',
  'ui.tab.cutlist': 'Cut list',
  'ui.projectName': 'Project name',
  'ui.new': 'New',
  'ui.importJson': 'Import JSON',
  'ui.exportJson': 'Export JSON',
  'ui.exportPdf': 'Export PDF',
  'ui.exporting': 'Exporting...',
  'ui.confirmNew': 'Replace the current project with the defaults?',
  'ui.lang.en': 'EN',
  'ui.lang.ru': 'RU',
  // sidebar
  'ui.section.envelope': 'Envelope',
  'ui.section.cabinet': 'Cabinet',
  'ui.section.columns': 'Columns',
  'ui.length': 'Length',
  'ui.heightMax': 'Height max',
  'ui.heightMin': 'Height min',
  'ui.depth': 'Depth',
  'ui.topClearance': 'Top clearance',
  'ui.tallSide': 'Tall side',
  'ui.side.left': 'Left',
  'ui.side.right': 'Right',
  'ui.gapBack': 'Min gap to back wall',
  'ui.panelThickness': 'Panel thickness',
  'ui.backThickness': 'Back thickness',
  'ui.plinthHeight': 'Plinth height',
  'ui.topStyle': 'Top style',
  'ui.top.sloped': 'Sloped',
  'ui.top.stepped': 'Stepped',
  'ui.column': 'Column {n}',
  'ui.width': 'Width',
  'ui.front': 'Front',
  'ui.front.none': 'Open',
  'ui.front.door': 'Door',
  'ui.front.drawers': 'Drawers',
  'ui.drawerCount': 'Drawers',
  'ui.rod': 'Hanging rod',
  'ui.shelves': 'Shelves',
  'ui.derived': 'top: tall {tall} / low {low} mm',
  'ui.moveLeft': 'Left',
  'ui.moveRight': 'Right',
  'ui.remove': 'Remove',
  'ui.addColumn': 'Add column',
  // 3D controls
  'ui.dims': 'Dims',
  'ui.envelope': 'Envelope',
  'ui.explode': 'Explode',
  // cut list table
  'table.num': '#',
  'table.part': 'Part',
  'table.col': 'Col',
  'table.qty': 'Qty',
  'table.length': 'Length',
  'table.width': 'Width',
  'table.thk': 'Thk',
  'table.material': 'Material',
  'table.notes': 'Notes',
  'table.total': 'Total parts',
  // toasts
  'toast.noRoom': 'No room for another column',
  'toast.imported': 'Project imported',
  'toast.importFailed': 'Import failed: {error}',
  'toast.pdfFailed': 'PDF export failed: {error}',
  // import errors
  'error.notJson': 'Not valid JSON',
  'error.badVersion': 'Expected a planner file with version {version}',
  'error.badShape': 'File does not contain a valid project',
  'error.failsValidation': 'Project fails validation: {reason}',
  // validation
  'error.length': 'Length must be > 0',
  'error.heightMax': 'Max height must be > 0',
  'error.depth': 'Depth must be > 0',
  'error.heightMin': 'Min height must be >= 0 and < max height',
  'error.topClearance': 'Top clearance must be >= 0',
  'error.panelThickness': 'Panel thickness must be > 0',
  'error.backThickness': 'Back thickness must be > 0',
  'error.plinthHeight': 'Plinth height must be >= 0',
  'error.gapBack': 'Back gap must be >= 0',
  'error.cabinetDepthMin': 'Cabinet depth must be >= 200',
  'error.cabinetDepthFit': 'Cabinet depth + min gap ({total}) exceeds envelope depth ({depth})',
  'error.columnsTotal': 'Columns total {total} exceeds envelope length {length}',
  'error.columnWidth': 'Column {n}: width must be >= {min}',
  'error.columnHeight': 'Column {n}: low-side height {h} is below minimum {min}',
  'error.drawerCount': 'Column {n}: at least 1 drawer',
  'error.shelves': 'Column {n}: shelves must be >= 0',
  // parts
  'part.sideL': 'Side L',
  'part.sideR': 'Side R',
  'part.top': 'Top',
  'part.bottom': 'Bottom',
  'part.back': 'Back',
  'part.shelf': 'Shelf',
  'part.door': 'Door',
  'part.drawerFront': 'Drawer front',
  'part.fixedFront': 'Fixed front',
  'part.plinth': 'Plinth',
  'part.rod': 'Rod',
  'material.panel': 'panel',
  'material.back': 'thin panel',
  'material.rod': 'tube',
  'note.bevelTopEdge': 'top edge bevel {deg}°',
  'note.bevelEndEdges': 'end edges bevel {deg}°',
  'note.trapezoid': 'trapezoid L {l} / R {r}',
  'note.rodDia': 'Ø{d} mm',
  // drawings
  'view.front': 'Front elevation',
  'view.plan': 'Plan',
  'view.side': 'Side section (tall end)',
  'view.columnDetail': 'Column {n} detail',
  'label.slope': 'slope {deg}°',
  'label.plinth': 'plinth {v}',
  'label.clear': 'clear {v}',
  'label.inner': 'inner {v}',
  'label.pitch': 'pitch {v}',
  'label.rod': 'rod {v}',
  'label.frontPanel': 'Front',
  'label.section': 'Section (low side)',
  // pdf
  'pdf.defaultTitle': 'Under-stairs cabinet',
  'pdf.scale': 'Scale 1:{n} (mm)',
  'pdf.date': 'Date',
  'pdf.envelopeLength': 'Envelope length',
  'pdf.heightMaxMin': 'Height max / min',
  'pdf.envelopeDepth': 'Envelope depth',
  'pdf.slope': 'Slope',
  'pdf.slopeValue': '{deg}°',
  'pdf.tallSide': 'Tall side',
  'pdf.topClearance': 'Top clearance',
  'pdf.cabinetDepthGap': 'Cabinet depth / min gap',
  'pdf.thickness': 'Panel / back thickness',
  'pdf.plinthHeight': 'Plinth height',
  'pdf.topStyle': 'Top style',
  'pdf.columns': 'Columns',
  'pdf.none': 'none',
  'pdf.drawersCount': 'x{n}',
  'pdf.shelvesCount': '+{n} shelves',
  'pdf.withRod': 'rod',
  'pdf.snapshotMissing': '(open the 3D tab before exporting to include a snapshot)',
  'pdf.snapshotFailed': '(3D snapshot unavailable)',
  'pdf.cutList': 'Cut list',
  'pdf.cutListCont': 'Cut list (cont. {n})',
  'pdf.total': 'Total parts: {n}',
};

export type MessageKey = keyof typeof en;
```

- [ ] **Step 4: Write src/i18n/ru.ts**

```ts
import type { MessageKey } from './en';

export const ru: Record<MessageKey, string> = {
  'ui.tab.3d': '3D',
  'ui.tab.front': 'Фасад',
  'ui.tab.plan': 'План',
  'ui.tab.side': 'Разрез',
  'ui.tab.cutlist': 'Спецификация',
  'ui.projectName': 'Название проекта',
  'ui.new': 'Новый',
  'ui.importJson': 'Импорт JSON',
  'ui.exportJson': 'Экспорт JSON',
  'ui.exportPdf': 'Экспорт PDF',
  'ui.exporting': 'Экспорт...',
  'ui.confirmNew': 'Заменить текущий проект проектом по умолчанию?',
  'ui.lang.en': 'EN',
  'ui.lang.ru': 'RU',
  'ui.section.envelope': 'Ниша',
  'ui.section.cabinet': 'Шкаф',
  'ui.section.columns': 'Секции',
  'ui.length': 'Длина',
  'ui.heightMax': 'Высота макс.',
  'ui.heightMin': 'Высота мин.',
  'ui.depth': 'Глубина',
  'ui.topClearance': 'Зазор сверху',
  'ui.tallSide': 'Высокая сторона',
  'ui.side.left': 'Слева',
  'ui.side.right': 'Справа',
  'ui.gapBack': 'Мин. зазор до стены',
  'ui.panelThickness': 'Толщина панели',
  'ui.backThickness': 'Толщина задней стенки',
  'ui.plinthHeight': 'Высота цоколя',
  'ui.topStyle': 'Тип верха',
  'ui.top.sloped': 'Скошенный',
  'ui.top.stepped': 'Ступенчатый',
  'ui.column': 'Секция {n}',
  'ui.width': 'Ширина',
  'ui.front': 'Фасад',
  'ui.front.none': 'Открытая',
  'ui.front.door': 'Дверь',
  'ui.front.drawers': 'Ящики',
  'ui.drawerCount': 'Кол-во ящиков',
  'ui.rod': 'Штанга',
  'ui.shelves': 'Полки',
  'ui.derived': 'верх: выс. {tall} / низ. {low} мм',
  'ui.moveLeft': 'Влево',
  'ui.moveRight': 'Вправо',
  'ui.remove': 'Удалить',
  'ui.addColumn': 'Добавить секцию',
  'ui.dims': 'Размеры',
  'ui.envelope': 'Ниша',
  'ui.explode': 'Разнести',
  'table.num': '№',
  'table.part': 'Деталь',
  'table.col': 'Секц.',
  'table.qty': 'Кол-во',
  'table.length': 'Длина',
  'table.width': 'Ширина',
  'table.thk': 'Толщ.',
  'table.material': 'Материал',
  'table.notes': 'Примечания',
  'table.total': 'Всего деталей',
  'toast.noRoom': 'Нет места для ещё одной секции',
  'toast.imported': 'Проект импортирован',
  'toast.importFailed': 'Ошибка импорта: {error}',
  'toast.pdfFailed': 'Ошибка экспорта PDF: {error}',
  'error.notJson': 'Некорректный JSON',
  'error.badVersion': 'Ожидается файл планировщика версии {version}',
  'error.badShape': 'Файл не содержит корректный проект',
  'error.failsValidation': 'Проект не проходит проверку: {reason}',
  'error.length': 'Длина должна быть > 0',
  'error.heightMax': 'Макс. высота должна быть > 0',
  'error.depth': 'Глубина должна быть > 0',
  'error.heightMin': 'Мин. высота должна быть >= 0 и < макс. высоты',
  'error.topClearance': 'Зазор сверху должен быть >= 0',
  'error.panelThickness': 'Толщина панели должна быть > 0',
  'error.backThickness': 'Толщина задней стенки должна быть > 0',
  'error.plinthHeight': 'Высота цоколя должна быть >= 0',
  'error.gapBack': 'Зазор до стены должен быть >= 0',
  'error.cabinetDepthMin': 'Глубина шкафа должна быть >= 200',
  'error.cabinetDepthFit': 'Глубина шкафа + мин. зазор ({total}) больше глубины ниши ({depth})',
  'error.columnsTotal': 'Сумма ширин секций {total} больше длины ниши {length}',
  'error.columnWidth': 'Секция {n}: ширина должна быть >= {min}',
  'error.columnHeight': 'Секция {n}: высота низкой стороны {h} меньше минимума {min}',
  'error.drawerCount': 'Секция {n}: минимум 1 ящик',
  'error.shelves': 'Секция {n}: полок должно быть >= 0',
  'part.sideL': 'Боковина Л',
  'part.sideR': 'Боковина П',
  'part.top': 'Крышка',
  'part.bottom': 'Дно',
  'part.back': 'Задняя стенка',
  'part.shelf': 'Полка',
  'part.door': 'Дверь',
  'part.drawerFront': 'Фасад ящика',
  'part.fixedFront': 'Глухая панель',
  'part.plinth': 'Цоколь',
  'part.rod': 'Штанга',
  'material.panel': 'панель',
  'material.back': 'тонкая панель',
  'material.rod': 'труба',
  'note.bevelTopEdge': 'скос верхней кромки {deg}°',
  'note.bevelEndEdges': 'скос торцов {deg}°',
  'note.trapezoid': 'трапеция Л {l} / П {r}',
  'note.rodDia': 'Ø{d} мм',
  'view.front': 'Фасад',
  'view.plan': 'План',
  'view.side': 'Разрез (высокая сторона)',
  'view.columnDetail': 'Секция {n} — деталировка',
  'label.slope': 'уклон {deg}°',
  'label.plinth': 'цоколь {v}',
  'label.clear': 'зазор {v}',
  'label.inner': 'внутр. {v}',
  'label.pitch': 'шаг {v}',
  'label.rod': 'штанга {v}',
  'label.frontPanel': 'Фасад',
  'label.section': 'Разрез (низкая сторона)',
  'pdf.defaultTitle': 'Шкаф под лестницей',
  'pdf.scale': 'Масштаб 1:{n} (мм)',
  'pdf.date': 'Дата',
  'pdf.envelopeLength': 'Длина ниши',
  'pdf.heightMaxMin': 'Высота макс. / мин.',
  'pdf.envelopeDepth': 'Глубина ниши',
  'pdf.slope': 'Уклон',
  'pdf.slopeValue': '{deg}°',
  'pdf.tallSide': 'Высокая сторона',
  'pdf.topClearance': 'Зазор сверху',
  'pdf.cabinetDepthGap': 'Глубина шкафа / мин. зазор',
  'pdf.thickness': 'Толщина панели / задней стенки',
  'pdf.plinthHeight': 'Высота цоколя',
  'pdf.topStyle': 'Тип верха',
  'pdf.columns': 'Секции',
  'pdf.none': 'нет',
  'pdf.drawersCount': '×{n}',
  'pdf.shelvesCount': '+{n} полок',
  'pdf.withRod': 'штанга',
  'pdf.snapshotMissing': '(откройте вкладку 3D перед экспортом, чтобы добавить снимок)',
  'pdf.snapshotFailed': '(3D-снимок недоступен)',
  'pdf.cutList': 'Спецификация',
  'pdf.cutListCont': 'Спецификация (продолжение {n})',
  'pdf.total': 'Всего деталей: {n}',
};
```

- [ ] **Step 5: Write src/i18n/index.ts**

```ts
import { en, type MessageKey } from './en';
import { ru } from './ru';

export type Lang = 'en' | 'ru';
export const LANGS: Lang[] = ['en', 'ru'];
export type { MessageKey };
export type Params = Record<string, string | number>;
export interface Msg { key: MessageKey; params?: Params }

const dicts: Record<Lang, Record<MessageKey, string>> = { en, ru };

export const msg = (key: MessageKey, params?: Params): Msg => (params ? { key, params } : { key });

export function t(lang: Lang, key: MessageKey, params?: Params): string {
  const s = dicts[lang][key] ?? dicts.en[key] ?? key;
  if (!params) return s;
  return s.replace(/\{(\w+)\}/g, (m, k: string) => (k in params ? String(params[k]) : m));
}

export const tm = (lang: Lang, m: Msg): string => t(lang, m.key, m.params);

export function isLang(v: unknown): v is Lang {
  return v === 'en' || v === 'ru';
}

export function detectLang(navLang: string | undefined): Lang {
  return navLang?.toLowerCase().startsWith('ru') ? 'ru' : 'en';
}
```

- [ ] **Step 6: Run test, typecheck**

Run: `pnpm vitest run src/i18n/i18n.test.ts && pnpm typecheck`
Expected: PASS (5 tests). A missing or extra key in `ru.ts` is a TypeScript error.

---

### Task 2: Model, persistence and store carry `Msg` and `lang`

**Files:**
- Modify: `src/model/types.ts`, `src/model/validate.ts`, `src/store/persist.ts`, `src/store/store.ts`
- Test: `src/model/validate.test.ts`, `src/store/store.test.ts` (modify)

**Interfaces:**
- Consumes: `Msg`, `msg`, `Lang`, `isLang`, `detectLang` (Task 1).
- Produces: `ValidationError { path; message: Msg }`; `ParseResult = { ok: true; project } | { ok: false; error: Msg }`; `LANG_KEY`, `loadLang(storage): Lang | null`, `saveLang(storage, lang)`; store `ui.lang: Lang`, `setLang(lang)`, `ui.toast: Msg | null`, `toast(m: Msg | null)`; `createPlannerStore(initial?, lang?)`; `startAutosave` also saves `lang` immediately when it changes.

- [ ] **Step 1: Update tests**

In `src/model/validate.test.ts`, add:
```ts
  it('messages are i18n Msg objects with params', () => {
    const p = defaultProject();
    p.cabinet.columns[1].width = 100;
    const e = validate(p).find((x) => x.path === 'cabinet.columns[1].width')!;
    expect(e.message).toEqual({ key: 'error.columnWidth', params: { n: 2, min: 136 } });
  });
```
In `src/store/store.test.ts`: change `expect(s.getState().ui.toast).toMatch(/No room/)` to `expect(s.getState().ui.toast).toEqual({ key: 'toast.noRoom' })`; change `if (!r.ok) expect(r.error).toMatch(/validation/)` to `if (!r.ok) expect(r.error.key).toBe('error.failsValidation')`; add:
```ts
  it('persists the language immediately and restores it', () => {
    const storage = memStorage();
    const s = createPlannerStore(defaultProject(), 'en');
    const stop = startAutosave(s, storage, 100);
    expect(s.getState().ui.lang).toBe('en');
    s.getState().setLang('ru');
    expect(loadLang(storage)).toBe('ru');
    stop();
    storage.setItem('understairs-planner:lang', 'xx');
    expect(loadLang(storage)).toBeNull();
  });
```
with `loadLang` imported from `./persist`.

- [ ] **Step 2: Run tests to verify they fail**

Run: `pnpm vitest run src/model/validate.test.ts src/store/store.test.ts`
Expected: FAIL (Msg shape, `setLang`, `loadLang` missing).

- [ ] **Step 3: Update src/model/types.ts**

```ts
import type { Msg } from '../i18n';
// ...
export interface ValidationError {
  path: string;
  message: Msg;
}
```

- [ ] **Step 4: Rewrite src/model/validate.ts**

```ts
import type { Project, ValidationError } from './types';
import { ceilY } from '../geometry/envelope';
import { msg, type MessageKey, type Params } from '../i18n';

export function validate(p: Project): ValidationError[] {
  const errors: ValidationError[] = [];
  const err = (path: string, key: MessageKey, params?: Params) => errors.push({ path, message: msg(key, params) });
  const { envelope: env, cabinet: cab } = p;

  if (!(env.length > 0)) err('envelope.length', 'error.length');
  if (!(env.heightMax > 0)) err('envelope.heightMax', 'error.heightMax');
  if (!(env.depth > 0)) err('envelope.depth', 'error.depth');
  if (!(env.heightMin >= 0 && env.heightMin < env.heightMax)) err('envelope.heightMin', 'error.heightMin');
  if (!(env.topClearance >= 0)) err('envelope.topClearance', 'error.topClearance');

  if (!(cab.panelThickness > 0)) err('cabinet.panelThickness', 'error.panelThickness');
  if (!(cab.backThickness > 0)) err('cabinet.backThickness', 'error.backThickness');
  if (!(cab.plinthHeight >= 0)) err('cabinet.plinthHeight', 'error.plinthHeight');
  if (!(cab.gapBack >= 0)) err('cabinet.gapBack', 'error.gapBack');
  if (!(cab.depth >= 200)) err('cabinet.depth', 'error.cabinetDepthMin');
  if (cab.depth + cab.gapBack > env.depth) {
    err('cabinet.depth', 'error.cabinetDepthFit', { total: cab.depth + cab.gapBack, depth: env.depth });
  }

  const total = cab.columns.reduce((s, c) => s + c.width, 0);
  if (total > env.length) err('cabinet.columns', 'error.columnsTotal', { total, length: env.length });

  const envelopeOk = env.length > 0 && env.heightMin >= 0 && env.heightMax > env.heightMin;
  const t = cab.panelThickness;
  const minWidth = 2 * t + 100;
  const minHeight = cab.plinthHeight + 2 * t + 100;
  let x = 0;
  cab.columns.forEach((c, i) => {
    const path = `cabinet.columns[${i}]`;
    const n = i + 1;
    if (!(c.width >= minWidth)) err(`${path}.width`, 'error.columnWidth', { n, min: minWidth });
    x += c.width;
    if (envelopeOk) {
      const hLow = ceilY(env, x) - env.topClearance;
      if (hLow < minHeight) err(`${path}.width`, 'error.columnHeight', { n, h: Math.round(hLow), min: minHeight });
    }
    if (c.front === 'drawers' && !(c.drawerCount >= 1)) err(`${path}.drawerCount`, 'error.drawerCount', { n });
    if (!(c.shelves >= 0)) err(`${path}.shelves`, 'error.shelves', { n });
  });
  return errors;
}
```

- [ ] **Step 5: Update src/store/persist.ts**

Replace the `ParseResult` type and the error returns, and add language persistence:
```ts
import { isLang, msg, t, tm, type Lang, type Msg } from '../i18n';
```
In `parseProjectShape`: `return { ok: false, error: msg('error.notJson') };`, `return { ok: false, error: msg('error.badVersion', { version: FILE_VERSION }) };`, `return { ok: false, error: msg('error.badShape') };`.
`parseProjectJson` must keep the *nested* validation message translatable, so a failure carries two fields:
```ts
export type ParseResult = { ok: true; project: Project } | { ok: false; error: Msg; reason?: Msg };
```
and `parseProjectJson` returns `{ ok: false, error: msg('error.failsValidation'), reason: errors[0].message }`. Callers render `tm(lang, error)` and, when `reason` is present, `t(lang, 'error.failsValidation', { reason: tm(lang, reason) })`. Add a helper in persist.ts:
```ts
export function parseErrorText(lang: Lang, r: { error: Msg; reason?: Msg }): string {
  return r.reason ? t(lang, r.error.key, { reason: tm(lang, r.reason) }) : tm(lang, r.error);
}
```
Then append:
```ts
export const LANG_KEY = 'understairs-planner:lang';

export function loadLang(storage: StorageLike): Lang | null {
  try {
    const v = storage.getItem(LANG_KEY);
    return isLang(v) ? v : null;
  } catch {
    return null;
  }
}

export function saveLang(storage: StorageLike, lang: Lang): void {
  try {
    storage.setItem(LANG_KEY, lang);
  } catch {
    // best-effort
  }
}
```

- [ ] **Step 6: Update src/store/store.ts**

```ts
import { detectLang, msg, type Lang, type Msg } from '../i18n';
import { loadFromStorage, loadLang, saveLang, saveToStorage, type StorageLike } from './persist';

export interface UiState {
  tab: Tab;
  showDims: boolean;
  explode: number; // 0..1
  showEnvelope: boolean;
  toast: Msg | null;
  lang: Lang;
}
```
In `PlannerState` add `setLang: (lang: Lang) => void;` and change `toast: (m: Msg | null) => void;`.
`createPlannerStore(initial: Project = defaultProject(), lang: Lang = 'en')`; initial `ui: { tab: '3d', showDims: true, explode: 0, showEnvelope: true, toast: null, lang }`; `addColumn` → `get().toast(msg('toast.noRoom'))`; add `setLang: (lang) => set((s) => ({ ui: { ...s.ui, lang } })),`; `toast: (m) => set((s) => ({ ui: { ...s.ui, toast: m } })),`.
In `startAutosave`, before the project check:
```ts
    if (s.ui.lang !== prev.ui.lang) saveLang(storage, s.ui.lang);
```
Bottom of file:
```ts
const browserStorage: StorageLike | null = typeof localStorage !== 'undefined' ? localStorage : null;
const initialLang: Lang =
  (browserStorage && loadLang(browserStorage)) ?? detectLang(typeof navigator !== 'undefined' ? navigator.language : undefined);
export const useStore = createPlannerStore((browserStorage && loadFromStorage(browserStorage)) ?? defaultProject(), initialLang);
if (browserStorage) startAutosave(useStore, browserStorage);
```

- [ ] **Step 7: Fix compile errors in consumers**

`src/ui/TopBar.tsx` and `src/ui/Sidebar.tsx` reference `r.error` / `e.message` as strings and `toast('...')` with strings; they are rewritten in Task 6. To keep the gate green now, make the minimal edits: in `TopBar.tsx` use `parseErrorText('en', r)` and `toast({ key: 'toast.imported' })` / `toast({ key: 'toast.importFailed', params: { error: parseErrorText('en', r) } })` / `toast({ key: 'toast.pdfFailed', params: { error: ... } })`; in `Sidebar.tsx` render `{tm('en', e.message)}`; in `Toast.tsx` render `{tm('en', msg)}`. (Task 6 replaces `'en'` with the hook.)

- [ ] **Step 8: Run the gate**

Run: `pnpm typecheck && pnpm test && pnpm build`
Expected: PASS (all existing tests plus the new ones).

---

### Task 3: Language-neutral parts and cut list

**Files:**
- Modify: `src/geometry/parts.ts`, `src/cutlist/cutlist.ts`
- Test: `src/geometry/parts.test.ts`, `src/cutlist/cutlist.test.ts` (modify)

**Interfaces:**
- Produces: `PartNameKey`, `Part { nameKey, index?, notes?: Msg[] }` (field `name` removed), `CutRow { nameKey, notes: Msg[] }` (field `name` removed).

- [ ] **Step 1: Update tests**

`src/geometry/parts.test.ts`:
- `expect(byId('col0-sideL').notes?.[0]).toMatch(/bevel 26\.6/)` → `expect(byId('col0-sideL').notes?.[0]).toEqual({ key: 'note.bevelTopEdge', params: { deg: 26.6 } })`
- `expect(top.notes?.[0]).toMatch(/end edges bevel 26\.6/)` → `expect(top.notes?.[0]).toEqual({ key: 'note.bevelEndEdges', params: { deg: 26.6 } })`
- in the back test add `expect(back.notes?.[0]?.key).toBe('note.trapezoid')` and `expect(back.nameKey).toBe('back')`
- in the shelves test add `expect(s.nameKey).toBe('shelf'); expect(s.index).toBe(1); expect(byId('col1-shelf3').index).toBe(3);`
- in the rod test replace nothing else, add `expect(rod.notes).toEqual([{ key: 'note.rodDia', params: { d: 25 } }])`
- drawer test add `expect(d[1].nameKey).toBe('drawerFront'); expect(d[1].index).toBe(2);`

`src/cutlist/cutlist.test.ts`: every `r.name === 'Bottom'` → `r.nameKey === 'bottom'`, `'Side L'` → `'sideL'`, `'Side R'` → `'sideR'`; `expect(fronts[0].name).toBe('Drawer front')` → `expect(fronts[0].nameKey).toBe('drawerFront')`; `expect(rod.notes).toEqual(['dia 25 mm'])` → `expect(rod.notes).toEqual([{ key: 'note.rodDia', params: { d: 25 } }])`; `expect(top.notes[0]).toMatch(/bevel/)` → `expect(top.notes[0].key).toBe('note.bevelEndEdges')`.

- [ ] **Step 2: Run tests to verify they fail**

Run: `pnpm vitest run src/geometry/parts.test.ts src/cutlist/cutlist.test.ts`
Expected: FAIL.

- [ ] **Step 3: Update src/geometry/parts.ts**

```ts
import { msg, type Msg } from '../i18n';

export type PartNameKey =
  | 'sideL' | 'sideR' | 'top' | 'bottom' | 'back' | 'shelf' | 'door' | 'drawerFront' | 'fixedFront' | 'plinth' | 'rod';

export interface Part {
  id: string;
  columnIndex: number | null;
  nameKey: PartNameKey;
  index?: number;       // 1-based for repeated parts (shelf, drawerFront)
  kind: PartKind;
  outline: Vec2[];
  thickness: number;
  transform: Transform;
  material: Material;
  notes?: Msg[];
}
```
In `buildParts`: `const bevelDeg = deg(theta);`; change `add` to
```ts
    const add = (
      key: string, nameKey: PartNameKey, kind: PartKind, outline: Vec2[], thickness: number,
      position: Vec3, rotation: Vec3, material: Material = 'panel', notes?: Msg[], index?: number,
    ) => parts.push({ id: `col${i}-${key}`, columnIndex: i, nameKey, index, kind, outline, thickness, transform: { position, rotation }, material, notes });
    const trap = (hL: number, hR: number) => (sloped ? [msg('note.trapezoid', { l: r1(hL), r: r1(hR) })] : undefined);
    const topEdge = sloped ? [msg('note.bevelTopEdge', { deg: bevelDeg })] : undefined;
```
and the calls:
```ts
    add('sideL', 'sideL', 'side', rect(cab.depth, L.sideLeftH), t, v3(x0 + t, plinth, 0), SIDE_ROT, 'panel', topEdge);
    add('sideR', 'sideR', 'side', rect(cab.depth, L.sideRightH), t, v3(x0 + w, plinth, 0), SIDE_ROT, 'panel', topEdge);
    add('bottom', 'bottom', 'bottom', ...same args...);
    // sloped top: notes [msg('note.bevelEndEdges', { deg: bevelDeg })]; stepped top: no notes
    add('top', 'top', 'top', ...);
    add('back', 'back', 'back', ..., 'back', trap(L.backLeftH, L.backRightH));
    L.shelfYs.forEach((y, k) => add(`shelf${k + 1}`, 'shelf', 'shelf', rect(...), t, v3(...), FLAT_ROT, 'panel', undefined, k + 1));
    if (L.doorOutline) add('door', 'door', 'door', ..., 'panel', trap(...));
    L.drawerFronts.forEach((d, k) => add(`drawer${k + 1}`, 'drawerFront', 'drawerFront', rect(...), t, v3(...), NO_ROT, 'panel', undefined, k + 1));
    if (L.fixedFront) add('fixedFront', 'fixedFront', 'fixedFront', f, t, v3(x0, plinth, -t), NO_ROT, 'panel', [msg('note.trapezoid', { l: r1(f[3].y - f[0].y), r: r1(f[2].y - f[1].y) })]);
    if (L.rodY !== null) add('rod', 'rod', 'rod', circle(...), L.interiorWidth, v3(...), ROD_ROT, 'rod', [msg('note.rodDia', { d: ROD_DIAMETER })]);
    add('plinth', 'plinth', 'plinth', rect(w, plinth), t, v3(x0, 0, PLINTH_SETBACK), NO_ROT);
```
Keep every geometric argument exactly as it is today; only the name/notes arguments change. Remove the now-unused `bevel` string.

- [ ] **Step 4: Update src/cutlist/cutlist.ts**

```ts
import type { Material, Part, PartKind, PartNameKey } from '../geometry/parts';
import type { Msg } from '../i18n';

export interface CutRow {
  nameKey: PartNameKey;
  kind: PartKind;
  columns: number[];
  qty: number;
  length: number;
  width: number;
  thickness: number;
  material: Material;
  notes: Msg[];
}
```
In `buildCutList`: remove `baseName`; `const key = [part.kind, part.nameKey, length, width, thickness, part.material, JSON.stringify(notes)].join('|');`; the new row uses `nameKey: part.nameKey`; the sort's second criterion becomes `a.nameKey.localeCompare(b.nameKey)`.

- [ ] **Step 5: Fix consumers to compile**

`src/pdf/exportPdf.ts` cut list page uses `r.name` and `r.notes.join('; ')`; `src/ui/CutListTable.tsx` likewise. Minimal edits now (Tasks 5 and 6 finish them): `t('en', \`part.${r.nameKey}\`)` and `r.notes.map((n) => tm('en', n)).join('; ')`. Note the template-literal key needs a cast: `t(lang, \`part.${r.nameKey}\` as MessageKey)`.

- [ ] **Step 6: Run the gate**

Run: `pnpm typecheck && pnpm test && pnpm build`
Expected: PASS.

---

### Task 4: Views take `lang`

**Files:**
- Modify: `src/drawing/views.ts`
- Test: `src/drawing/views.test.ts` (modify)

**Interfaces:**
- Produces: `frontView(p, lang = 'en')`, `planView(p, lang = 'en')`, `sideView(p, lang = 'en')`, `columnDetail(p, index, lang = 'en')`.

- [ ] **Step 1: Update tests**

In `src/drawing/views.test.ts` change `expect(texts(d)).toContain('slope 26.6 deg')` to `expect(texts(d)).toContain('slope 26.6°')` and add:
```ts
describe('russian views', () => {
  const p = defaultProject();
  it('translate titles and labels', () => {
    expect(frontView(p, 'ru').title).toBe('Фасад');
    expect(texts(frontView(p, 'ru'))).toContain('уклон 26.6°');
    expect(planView(p, 'ru').title).toBe('План');
    expect(sideView(p, 'ru').title).toBe('Разрез (высокая сторона)');
    const d = columnDetail(p, 1, 'ru');
    expect(d.title).toBe('Секция 2 — деталировка');
    expect(texts(d)).toContain('Фасад');
    expect(hasDim(d, 564)).toBe(true); // geometry unchanged
  });
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `pnpm vitest run src/drawing/views.test.ts`
Expected: FAIL.

- [ ] **Step 3: Update src/drawing/views.ts**

Add `import { t, type Lang } from '../i18n';`. Every exported view gets a trailing `lang: Lang = 'en'` parameter and uses:
- frontView: plinth dim label `t(lang, 'label.plinth', { v: fmtLen(cab.plinthHeight) })`; slope text `t(lang, 'label.slope', { deg: deg(slopeAngle(env)) })`; title `t(lang, 'view.front')`.
- planView: `t(lang, 'label.clear', { v: fmtLen(env.depth - cab.depth) })`; title `t(lang, 'view.plan')`.
- sideView: clear + plinth labels as above; title `t(lang, 'view.side')`.
- columnDetail: `label.inner` (twice), `label.pitch`, `label.rod`, texts `label.frontPanel` and `label.section`; title `t(lang, 'view.columnDetail', { n: index + 1 })`.
No geometry changes.

- [ ] **Step 4: Run the gate**

Run: `pnpm typecheck && pnpm test && pnpm build`
Expected: PASS.

---

### Task 5: PDF font and language

**Files:**
- Create: `scripts/embed-font.mjs`, `src/pdf/fonts/PT_Sans-Web-Regular.ttf`, `src/pdf/fonts/OFL.txt`, `src/pdf/fonts/ptsans.ts` (generated), `src/pdf/font.ts`
- Modify: `src/pdf/exportPdf.ts`, `package.json` (script `embed-font`)
- Test: `src/pdf/exportPdf.test.ts` (modify)

**Interfaces:**
- Produces: `registerPdfFont(doc: jsPDF): void` (adds PT Sans to the VFS, registers family `PTSans`, sets it); `PdfOptions { lang?: Lang; snapshotPng?; date? }`; `buildPdf(p, opts)` translated.

- [ ] **Step 1: Fetch the font**

```bash
mkdir -p src/pdf/fonts
curl -fsSL -o src/pdf/fonts/PT_Sans-Web-Regular.ttf https://raw.githubusercontent.com/google/fonts/main/ofl/ptsans/PT_Sans-Web-Regular.ttf
curl -fsSL -o src/pdf/fonts/OFL.txt https://raw.githubusercontent.com/google/fonts/main/ofl/ptsans/OFL.txt
ls -l src/pdf/fonts
```
Expected: a TTF of roughly 250-400 KB and the licence text. If the URL 404s, use `https://raw.githubusercontent.com/googlefonts/roboto-2/main/src/hinted/Roboto-Regular.ttf` (Apache-2.0; then name the family `Roboto` everywhere below and fetch `https://raw.githubusercontent.com/googlefonts/roboto-2/main/LICENSE` instead of OFL.txt).

- [ ] **Step 2: Write scripts/embed-font.mjs and generate the module**

```js
// Usage: node scripts/embed-font.mjs  -> writes src/pdf/fonts/ptsans.ts
import { readFileSync, writeFileSync } from 'node:fs';
const ttf = readFileSync('src/pdf/fonts/PT_Sans-Web-Regular.ttf');
const b64 = ttf.toString('base64');
writeFileSync(
  'src/pdf/fonts/ptsans.ts',
  `// Generated by scripts/embed-font.mjs from PT_Sans-Web-Regular.ttf (SIL OFL 1.1, see OFL.txt). Do not edit.\nexport const PTSANS_REGULAR_BASE64 =\n  '${b64}';\n`,
);
console.log(`wrote src/pdf/fonts/ptsans.ts (${b64.length} chars)`);
```
Add `"embed-font": "node scripts/embed-font.mjs"` to `package.json` scripts and run `pnpm embed-font`.

- [ ] **Step 3: Write failing test additions in src/pdf/exportPdf.test.ts**

```ts
  it('builds the Russian document with the embedded font and the same page count', () => {
    const p = defaultProject();
    const en = buildPdf(p, { lang: 'en' });
    const ru = buildPdf(p, { lang: 'ru' });
    expect(ru.getNumberOfPages()).toBe(en.getNumberOfPages());
    expect(Object.keys(ru.getFontList())).toContain('PTSans');
    expect(ru.getFont().fontName).toBe('PTSans');
  });
```

- [ ] **Step 4: Run test to verify it fails**

Run: `pnpm vitest run src/pdf/exportPdf.test.ts`
Expected: FAIL (`PTSans` not in font list).

- [ ] **Step 5: Write src/pdf/font.ts**

```ts
import type { jsPDF } from 'jspdf';
import { PTSANS_REGULAR_BASE64 } from './fonts/ptsans';

export const PDF_FONT = 'PTSans';

/** Embed PT Sans (Cyrillic + Latin) and make it the current font. */
export function registerPdfFont(doc: jsPDF): void {
  doc.addFileToVFS('PT_Sans-Web-Regular.ttf', PTSANS_REGULAR_BASE64);
  doc.addFont('PT_Sans-Web-Regular.ttf', PDF_FONT, 'normal');
  doc.setFont(PDF_FONT, 'normal');
}
```

- [ ] **Step 6: Update src/pdf/exportPdf.ts**

```ts
import { t, tm, type Lang, type MessageKey } from '../i18n';
import { registerPdfFont } from './font';

export interface PdfOptions { lang?: Lang; snapshotPng?: string | null; date?: Date }

export function buildPdf(p: Project, opts: PdfOptions = {}): jsPDF {
  const lang = opts.lang ?? 'en';
  const doc = new jsPDF({ orientation: 'landscape', unit: 'mm', format: 'a4' });
  registerPdfFont(doc);
  summaryPage(doc, p, opts, lang);
  for (const d of [frontView(p, lang), planView(p, lang), sideView(p, lang)]) { doc.addPage(); drawingPage(doc, d, lang); }
  layoutColumns(p).forEach((_, i) => { doc.addPage(); drawingPage(doc, columnDetail(p, i, lang), lang); });
  cutListPages(doc, buildCutList(buildParts(p)), lang);
  return doc;
}
```
`drawingPage(doc, d, lang)`: footer `t(lang, 'pdf.scale', { n: N })`.
`summaryPage(doc, p, opts, lang)`: title `p.name || t(lang, 'pdf.defaultTitle')`; columns descriptor:
```ts
  const columns = cab.columns
    .map((c, i) => [
      `${i + 1}: ${c.width} ${t(lang, `ui.front.${c.front}` as MessageKey)}`,
      c.front === 'drawers' ? t(lang, 'pdf.drawersCount', { n: c.drawerCount }) : '',
      c.shelves ? t(lang, 'pdf.shelvesCount', { n: c.shelves }) : '',
      c.rod && c.front !== 'drawers' ? t(lang, 'pdf.withRod') : '',
    ].filter(Boolean).join(' '))
    .join('; ');
  const rows: [string, string][] = [
    [t(lang, 'pdf.date'), date],
    [t(lang, 'pdf.envelopeLength'), `${env.length}`],
    [t(lang, 'pdf.heightMaxMin'), `${env.heightMax} / ${env.heightMin}`],
    [t(lang, 'pdf.envelopeDepth'), `${env.depth}`],
    [t(lang, 'pdf.slope'), t(lang, 'pdf.slopeValue', { deg: deg(slopeAngle(env)) })],
    [t(lang, 'pdf.tallSide'), t(lang, `ui.side.${env.tallSide}` as MessageKey)],
    [t(lang, 'pdf.topClearance'), `${env.topClearance}`],
    [t(lang, 'pdf.cabinetDepthGap'), `${cab.depth} / ${cab.gapBack}`],
    [t(lang, 'pdf.thickness'), `${cab.panelThickness} / ${cab.backThickness}`],
    [t(lang, 'pdf.plinthHeight'), `${cab.plinthHeight}`],
    [t(lang, 'pdf.topStyle'), t(lang, `ui.top.${cab.topStyle}` as MessageKey)],
    [t(lang, 'pdf.columns'), columns || t(lang, 'pdf.none')],
  ];
```
Snapshot texts: `t(lang, 'pdf.snapshotFailed')` / `t(lang, 'pdf.snapshotMissing')`.
`cutListPages(doc, rows, lang)`: header labels from `table.*` keys (`['#'...]` → `[t(lang,'table.num'), M], [t(lang,'table.part'), M + 10], ...`); page title `t(lang, 'pdf.cutList')` / `t(lang, 'pdf.cutListCont', { n: page + 1 })`; row values `t(lang, \`part.${r.nameKey}\` as MessageKey)`, `t(lang, \`material.${r.material}\` as MessageKey)`, `r.notes.map((n) => tm(lang, n)).join('; ').slice(0, 55)`; footer `t(lang, 'pdf.total', { n: total })`.
Remove the `doc.setFont('helvetica', 'normal')` line.

- [ ] **Step 7: Run the gate**

Run: `pnpm typecheck && pnpm test && pnpm build`
Expected: PASS. The build output grows by roughly the base64 font size; the >500 kB chunk warning is expected.

---

### Task 6: UI translation and language switch

**Files:**
- Create: `src/ui/useT.ts`
- Modify: `src/ui/TopBar.tsx`, `src/ui/Sidebar.tsx`, `src/ui/ColumnCard.tsx`, `src/ui/CutListTable.tsx`, `src/ui/Toast.tsx`, `src/ui/View2D.tsx`, `src/ui/three/Viewport3D.tsx`, `src/styles.css`, `README.md`

**Interfaces:**
- Produces: `useT(): { lang, t: (key, params?) => string, tm: (m: Msg) => string }`.

- [ ] **Step 1: Write src/ui/useT.ts**

```ts
import { useStore } from '../store/store';
import { t, tm, type Lang, type MessageKey, type Msg, type Params } from '../i18n';

export function useT(): { lang: Lang; t: (key: MessageKey, params?: Params) => string; tm: (m: Msg) => string } {
  const lang = useStore((s) => s.ui.lang);
  return { lang, t: (key, params) => t(lang, key, params), tm: (m) => tm(lang, m) };
}
```

- [ ] **Step 2: Translate every component**

Replace each literal string with `t('<key>')` per the dictionary in Task 1. Specifics:
- `TopBar.tsx`: `TABS` labels → keys `ui.tab.*` (look up with `t` inside the render); name placeholder `ui.projectName`; `window.confirm(t('ui.confirmNew'))`; buttons `ui.new`, `ui.importJson`, `ui.exportJson`, `ui.exportPdf` / `ui.exporting`; toasts `toast({ key: 'toast.imported' })`, `toast({ key: 'toast.importFailed', params: { error: parseErrorText(lang, r) } })`, `toast({ key: 'toast.pdfFailed', params: { error: message } })`; PDF export passes `{ lang, snapshotPng }`. Add the language switch after the tabs:
```tsx
      <nav className="tabs lang">
        {LANGS.map((l) => (
          <button key={l} className={l === lang ? 'active' : ''} onClick={() => setLang(l)}>{t(`ui.lang.${l}` as MessageKey)}</button>
        ))}
      </nav>
```
with `setLang` taken from `useStore.getState()` like the other actions.
- `Sidebar.tsx`: section titles, every field label, select option labels (`ui.side.*`, `ui.top.*`), `ui.addColumn`; errors `{tm(e.message)}`.
- `ColumnCard.tsx`: `t('ui.column', { n: index + 1 })`, `ui.width`, `ui.front` + options `ui.front.*`, `ui.drawerCount`, `ui.rod`, `ui.shelves`, derived line `t('ui.derived', { tall: fmt(range.hTall), low: fmt(range.hLow) })`, buttons `ui.moveLeft` / `ui.moveRight` / `ui.remove`.
- `CutListTable.tsx`: headers `table.*`; cells `t(\`part.${r.nameKey}\` as MessageKey)`, `t(\`material.${r.material}\` as MessageKey)`, `r.notes.map(tm).join('; ')`; footer `table.total`.
- `Toast.tsx`: `{tm(msg)}`.
- `View2D.tsx`: pass `lang` to the view functions and add `lang` to the `useMemo` deps.
- `Viewport3D.tsx`: control labels `ui.dims`, `ui.envelope`, `ui.explode`.
- `styles.css`: `.tabs.lang { margin-left: 4px; }` and `.tabs.lang button { padding: 6px 8px; font-size: 12px; }`.
- `README.md`: add a line "Languages: English and Russian (switch in the top bar; the PDF embeds PT Sans, SIL OFL)."

- [ ] **Step 3: Grep for leftovers**

Run: `grep -rn "'en'" src/ui src/pdf/exportPdf.ts` — the only remaining `'en'` literals must be defaults in function signatures or the `LANGS` array, not hard-coded call sites. Also `grep -rn "Column \|Cut list\|Export PDF\|No room" src/ui` should return nothing.

- [ ] **Step 4: Run the gate and the dev-server check**

Run: `pnpm typecheck && pnpm test && pnpm build`, then `pnpm dev` in the background and curl `http://localhost:5173/src/ui/TopBar.tsx` to confirm it transforms; stop the server.

Manual checks for the user (no browser in agents): switch to RU — sidebar, tabs, buttons, cut list and 2D drawings are Russian; reload keeps RU; Export PDF in RU shows Cyrillic on every page including the cut list; switch back to EN.
